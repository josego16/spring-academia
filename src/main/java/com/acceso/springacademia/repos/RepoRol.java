package com.acceso.springacademia.repos;

import com.acceso.springacademia.modelos.Rol;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepoRol extends JpaRepository<Rol, Long> {
}
